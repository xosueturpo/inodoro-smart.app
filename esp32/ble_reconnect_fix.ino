// ESP32 = puente transparente Flutter → Arduino UNO
// Solo procesa localmente: RESET y SSID|PASSWORD
// Todo lo demás (OFF R G B Y M C W) → Serial2 → UNO
// UART: GPIO17→D2, GPIO16←D3, GND↔GND @ 9600
